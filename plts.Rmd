---
title: "plts"
output: html_document
---

Here's the tidyverse

```{r}
library(tidyverse)
library(ggplot2)
```


```{r}
plot_df <-
  tibble(
    x = rnorm(100,mean=1, sd=7),
    y= 1+2*x +rnorm(100)
  )

```

```{r}
plot_df |>
  ggplot(aes(x=x, y=y)) +
  geom_point()
```